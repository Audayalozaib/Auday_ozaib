# .env.example

# This project is configured to run on Railway.
# The DATABASE_URL variable is set automatically in the Railway dashboard.

# If you want to run this project locally (e.g., with XAMPP, Laragon, Docker),
# copy this file to .env and fill in your local database details below.

DB_HOST=
DB_NAME=
DB_USER=
DB_PASS=
